 The output should look exactly like this:
  ["Red", "Blue", "Green", "Yellow"]
This list contains four color names. Make sure your program prints them in the same order.

# Example usage:
print(colors)
improved version
day- task prompt
along with the web pages